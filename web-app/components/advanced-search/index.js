import AdvancedSearchWrapper from "./advanced-search-wrapper";
import { ReactElement } from "react";

/**
 * Responsible for rendering the AdvancedSearch area
 * 
 * Logic is contained within the wrapper, just returns the wrapper.
 * 
 * @returns {ReactElement}
 */
export default function AdvancedSearch() {
    return <AdvancedSearchWrapper />
}